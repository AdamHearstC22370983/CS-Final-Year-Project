# Web framework
fastapi
uvicorn[standard]
python-multipart

# Database
sqlalchemy
psycopg2-binary

# Validation
pydantic
email-validator

# Password hashing
passlib[bcrypt]

# Document parsing
python-docx
pdfplumber

# NLP
spacy

# HTTP requests
requests

# Optional: used by preprocessing / filtering scripts
ijson

After installing the above requirements, run 
"python -m spacy download en_core_web_sm" in the python environment.

# Optional: used if TF-IDF / cosine recommender files are enabled
scikit-learn
